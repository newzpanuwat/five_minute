<h1>Add Author</h1>

<%= form_for (@author) do |f| %>

    <div class="field">
        <%= f.label :name %>
        <%= f.text_field :name %><br />
    </div>

    <div class="action">
    <%= f.submit "Create author" %>
    </div>

<% end %>

<%= link_to 'Back', authors_path %>